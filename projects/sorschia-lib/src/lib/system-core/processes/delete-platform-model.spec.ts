import { DeletePlatformModel } from './delete-platform-model';

describe('DeletePlatformModel', () => {
  it('should create an instance', () => {
    expect(new DeletePlatformModel()).toBeTruthy();
  });
});
